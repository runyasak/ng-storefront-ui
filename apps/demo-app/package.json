{
  "name": "demo-app",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "ng": "ng",
    "serve": "ng serve",
    "build:demo": "ng build",
    "watch": "ng build --watch --configuration development",
    "test": "ng test",
    "lint": "ng lint"
  }
}
